#include "main.h"
/**
 * print_last_digit - prints the last digit of a number
 * @x: the integer to extract the last digit from
 * Return: value of the last digit
 */
int print_last_digit(int x)
{
	int a;

	a = x % 10;
	{
		_putchar(a);
	}
		_putchar('\n');
		return (x);
}

