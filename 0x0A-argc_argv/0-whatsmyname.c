#include "main.h"
#include <stdio.h>
/**
  *main - Entry point
  *@argc: number of arguments
  *@argv: array pointer to argument
  *Return: 0
  */
int main(int argc, char *argv[])
{
	if (argc)
		printf("%s\n", argv[0]);
	return (0);
}
